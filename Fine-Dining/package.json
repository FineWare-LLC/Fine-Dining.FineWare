{
  "name": "fine-dining",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://fine-dining.fineware.tech",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "clean": "rm -rf dist",
    "preview": "vite preview",
    "predeploy": "npm run clean && vite build && echo 'fine-dining.fineware.tech' > dist/CNAME && cp dist/index.html dist/404.html",
    "deploy": "gh-pages -d dist --dotfiles",
    "postdeploy": "echo 'fine-dining.fineware.tech' > dist/CNAME"

  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
